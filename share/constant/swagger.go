package constant

const (
	// SwaggerUIPath swagger-ui路径
	SwaggerUIPath = "swaggerui/dist"
	// SwaggerPath swagger路径
	SwaggerPath = "docs/swagger"
	// SwaggerStaticPath swagger-ui static路径
	SwaggerStaticPath = "/static"
	// SwaggerJsonPath swagger json路径
	SwaggerJsonPath = "/swagger"
)
